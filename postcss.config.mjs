// Tailwind v4 PostCSS plugin — REQUIRED, or @import "tailwindcss" is ignored
// and the site renders with zero styles (the "white 90s page").
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
